"use client";

import ActivitySelector from "@/components/activities/ActivitySelector";

/**
 * Mount point kept from Task 01 so the approved Hero is untouched.
 * The strip itself is Task 02 — see components/activities/.
 */
export default function SectorStrip() {
  return <ActivitySelector />;
}
